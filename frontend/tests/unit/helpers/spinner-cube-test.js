import { spinnerCube } from 'aau-food-frontend/helpers/spinner-cube';
import { module, test } from 'qunit';

module('Unit | Helper | spinner cube');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spinnerCube([42]);
  assert.ok(result);
});
