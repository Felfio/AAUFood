import { formatEuro } from 'aau-food-frontend/helpers/format-euro';
import { module, test } from 'qunit';

module('Unit | Helper | format euro');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatEuro([42]);
  assert.ok(result);
});
