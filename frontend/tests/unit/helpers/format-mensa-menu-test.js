import { formatMensaMenu } from 'aau-food-frontend/helpers/format-mensa-menu';
import { module, test } from 'qunit';

module('Unit | Helper | format mensa menu');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatMensaMenu([42]);
  assert.ok(result);
});
