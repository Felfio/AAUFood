version: '2'
services:
  redis:
    image: "redis:alpine"
    restart: always

  node:
    build: .
    restart: always
    volumes:
     - ./app:/usr/src/aaufood/app
    ports:
     - "${FOOD_PORT}:3000"
    depends_on:
     - redis
    links:
     - redis
