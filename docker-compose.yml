version: '2'
services:
  redis:
    image: "redis:alpine"

  node:
    build: .
    volumes:
     - .:/usr/src/app
     - /usr/src/app/dist
     - /usr/src/app/public/dist
    ports:
     - "${FOOD_PORT}:3000"
    depends_on:
     - redis
    links:
     - redis
