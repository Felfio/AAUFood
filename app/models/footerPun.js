"use strict";

class FooterPun {
    constructor(name, preText, icon, iconLink, postText) {
        this.name = name;
        this.preText = preText;
        this.icon = icon;
        this.iconLink = iconLink;
        this.postText = postText;
    }
}

module.exports = FooterPun;